h1 Members Only - Private Club
div(class="register")
  if user
    ul 
      li Welcome #{user.fullName}
      li
        a(href="/status")
          button(type="submit") GET VIP STATUS 
      li
        form(method="POST" action="/logout")
          button(type="submit") Log out
  else 
    ul 
      li 
        a(href="/login") Log In
      li
        a(href="/register") Sign Up
